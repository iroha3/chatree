# Roadmap

> **只放「还没做的」** —— 一句一条，能一眼扫完；做完就删条目。
> **决策的理由在 `docs/DECISIONS.md`**（`→ D-00x`），本文件不写论证。
> 历史看 `CHANGELOG.md`，契约 / 约定 / 坑看 `DEV.md`。
>
> 状态：`📋 待做` · `🟡 讨论中` · `⏸ 押后` · `🚫 明确不做（理由见 DECISIONS）`

---

## 桌面 / 分发

- ⏸ **Windows ARM64 包**：Pake 文件名 bug 导致必挂。→ D-011
- 🟡 **更新方式升级**：现在是「关于页查 latest release + 下载链接」的半自动；静默自动更新
  需要 Tauri updater + 更新包签名 + 自己维护 `src-tauri`，押后。
- 📋 **首次进桌面版的数据引导**：桌面 origin（`http://tauri.localhost`）和浏览器不共享
  IndexedDB。新用户第一次打开桌面版时，主动提示「从浏览器导出的 JSON 可在此导入」。

## 性能

- 📋 **流式输出时的画布闪烁**：根因已定位 —— `ChatFlow` 的 `onChunk`/`onReasoning` 每个 SSE
  分片都 `setState`（50–100 次/秒），`md-editor-rt` 把整篇 Markdown 重新解析并换掉整块 DOM。
  方案：合并到 ~80ms 一批（**收尾必须补一次 flush**），必要时流式期间关掉高亮。
- 📋 **流式过程节流落盘**：回答是生成完才落盘的，`streamingResponses` 只在内存里，**刷新会丢掉
  已生成的一半**。建议每 ~500ms 或每 200 字写一次。（「停止」已经会保存已生成部分，但刷新不会。）
- 📋 **打包瘦身**：首屏 JS ~392KB gzip（highlight.js ~56KB、katex ~78KB）。
  katex 改成动态 import，只在回答里真的出现公式时才加载。

## 正确性 / 测试

- 🟡 **发版的 `concurrency` 会误杀正在跑的构建**：见 `DEV.md` §8.1 的 ⚠️。
  `cancel-in-progress: true` 是工作流级的，发版跑着时**任何** push 都会把它取消。
  初步修法：闸门拆成独立 workflow，或去掉 `cancel-in-progress`。待实测后定。
- 📋 **补自动测试**：分支上下文组装、持久化、SSE 解析、`sessionTransfer` 导入校验、
  `sessionStore.importSessions` 去重、`computeChildPosition` 落点不重叠。
- 📋 **假 SSE 服务器的端到端测试**：现在没覆盖「流式请求 → 停止 → 已生成内容落盘」。
  起一个本地 SSE server，把模型 baseURL 指过去，就能测。
- 📋 **推理模型兼容层**：不同 provider 的 thinking 流字段/格式不完全一致
  （`reasoning_content` 之外还有别的写法），需要一个显式的适配层。

## 交互

- ⏸ **多个根 / 多个系统提示词节点同屏** → D-001
- ⏸ **会话并排 / 分屏** → D-002
- ⏸ **快捷键补齐**：动手前先读 `DEV.md` §3.13 和 → D-004（本应用是**有意**避开全局快捷键的）。
- ⏸ **复制整条路径为 Markdown**：「根 → 本节点」整条对话导出为 Markdown。
- ⏸ **子树折叠**：节点太多时折叠整棵子树。
- 🟡 **长内容的折叠边界** → D-005
- 🟡 **从上游 `Anionex/treeAI` 合并** → D-006
- 📋 **复制会话**：把一棵会话（含子树）原样复制成新会话，用于试错 / 留底 / A-B。
- 📋 **跨会话 token / 花费累计**
- 🟡 **连续阅读（沿一条路径从头读到尾）**：现在只能双击单个节点看浮层。设想把「根 → 某节点」
  这条路径上的问答拼成一篇可滚动的连续文本，自动滚到目标节点。
  **先决条件**：把「从节点提取路径」做成纯函数 —— 连续阅读 /「复制路径为 Markdown」/
  「导出线性文档」三处共用。
- 🟡 **纯单链（整条会话无分叉）的布局本身**：用户的痛点是「聊 20 轮变一长串根本没法看」。
  查下来主因**不是布局，是「走不动」**（滚轮被设成缩放，只能拖着走）—— 已改成滚轮平移，
  见 `DEV.md` §3.14 与 → D-012。剩下「笔直一串」到底还难不难看，**等真用过再判断**；
  别凭空改常量（候选：缩 `V_GAP` / 折成蛇形 / 阅读视图；回归断言在 `tests/node-ux-check.mjs`）。
- 🟡 **价值提炼**：把一棵对话树提炼成「结论 / 要点」。形态未定 —— 输入是整棵树 / 当前路径 /
  手选节点？输出落在哪（新节点 / 会话描述 / 导出文档）？要不要落库（落库会进导出格式）？

## 导出

- ⏸ **其它导出格式（PDF / DOCX / 独立 HTML / Markdown）** → D-003
  - ✅ 已完成：右上角平级的「导出 JSON」+「思维导图」两个图标，已合成一个**分享菜单**。
- 📋 **PDF 技术选型（`window.print()` vs pdfmake）** → D-003
- 📋 **DOCX** → D-003

## 移动端（大件，方向未定）

- 🟡 **先拍方向**：A = 只读 + 单节点「路径视图」（轻量）；B = 全画布 + 捏合缩放（重）。
- 验收清单（做到哪条算哪条）：
  - [ ] ≤768px 侧栏变抽屉，不遮挡画布
  - [ ] 卡片宽度自适应，不再横向滚动
  - [ ] 单节点路径视图可读、上下滑动顺畅
  - [ ] 滑条 / 输入框不会误拖画布
  - [ ] 软键盘弹出时输入框不被遮

---

## 🚫 明确不做（别再提）

**理由全在 `docs/DECISIONS.md`：**

- 代码签名 → D-007
- 单文件 HTML（把整站塞进一个 `.html`）→ D-008
- 兄弟分支切换器（`← 2/3 →`）→ D-009
- 标签 / 云同步 / 插件化 / mermaid / 纯美化 → D-010
