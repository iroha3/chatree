# Roadmap

> **只放「还没做的」** —— 一句一条，能一眼扫完；做完就删条目。
> **决策的理由在 `docs/DECISIONS.md`**（`→ D-00x`），本文件不写论证。
> 历史看 `CHANGELOG.md`，契约 / 约定 / 坑看 `DEV.md`。
>
> 状态：`📋 待做` · `🟡 讨论中` · `🚫 明确不做（理由见 DECISIONS）`
>
> ⛔ **`⏸ 押后` 的东西不进本文件**（清单在文末）。押后 = 「不打算做，要做的时候再从
> `DECISIONS.md` 里拎出来」；列在这里只会让路线图看着有一堆永远不动的条目。

---

## 桌面 / 分发

- 🟡 **更新方式升级**：现在是「关于页查 latest release + 下载链接」的半自动；静默自动更新
  需要 Tauri updater + 更新包签名 + 自己维护 `src-tauri`，押后。
- 🟡 **首次进桌面版的数据引导**：桌面 origin 和浏览器不共享 IndexedDB，新用户打开是一片
  空白，容易以为数据丢了。→ D-016

## 性能

- 📋 **流式过程节流落盘**：回答是生成完才落盘的，`streamingResponses` 只在内存里，**刷新会丢掉
  已生成的一半**。建议每 ~500ms 或每 200 字写一次。（「停止」已经会保存已生成部分，但刷新不会。）
- 📋 **打包瘦身**：首屏 JS ~392KB gzip（highlight.js ~56KB、katex ~78KB）。
  katex 改成动态 import，只在回答里真的出现公式时才加载。

## 正确性 / 测试

- 🟡 **发版的 `concurrency` 会误杀正在跑的构建**：见 `DEV.md` §8.1 的 ⚠️。
  `cancel-in-progress: true` 是工作流级的，发版跑着时**任何** push 都会把它取消 ——
  v0.2.0 那次发版就是这样被取消掉的。初步修法：闸门拆成独立 workflow，
  或去掉 `cancel-in-progress`。待实测后定。
- 📋 **补自动测试**：分支上下文组装、持久化、SSE 解析、`sessionTransfer` 导入校验、
  `sessionStore.importSessions` 去重、`computeChildPosition` 落点不重叠。
- 📋 **假 SSE 服务器的端到端测试**：现在没覆盖「流式请求 → 停止 → 已生成内容落盘」。
  起一个本地 SSE server，把模型 baseURL 指过去，就能测。
- 📋 **推理模型兼容层**：不同 provider 的 thinking 流字段/格式不完全一致
  （`reasoning_content` 之外还有别的写法），需要一个显式的适配层。

## 交互

- 📋 **长内容的折叠边界**：思考链形态已定，系统提示词 / LLM 回答还没动手。→ D-005
- 🟡 **复制会话**：把一棵会话（含子树）原样复制成新会话，用于试错 / 留底 / A-B。待定。
- 🟡 **复制整条路径为 Markdown**：「根 → 本节点」整条对话导出为 Markdown。待定。
  （路径提取的纯函数已在 `utils/tree.ts`，连续阅读用的就是它）
- ⏸ **子树折叠**：节点太多时折叠整棵子树。

## 移动端（后续版本推进）

- 🟡 **动手前先拍方向**：A = 只读 + 单节点「路径视图」（轻量）；B = 全画布 + 捏合缩放（重）。
- 验收清单（做到哪条算哪条）：
  - [ ] ≤768px 侧栏变抽屉，不遮挡画布
  - [ ] 卡片宽度自适应，不再横向滚动
  - [ ] 单节点路径视图可读、上下滑动顺畅
  - [ ] 滑条 / 输入框不会误拖画布
  - [ ] 软键盘弹出时输入框不被遮

---

## ⏸ 押后（不打算做；真要做再从 DECISIONS 拎出来）

多根同屏 → D-001 · 会话分屏 → D-002 · 其它导出格式（PDF / DOCX / Markdown / 独立 HTML）
→ D-003 · 快捷键补齐 → D-004 · Windows ARM64 包 → D-011

## 🚫 明确不做（别再提）

**理由全在 `docs/DECISIONS.md`：**

- 代码签名 → D-007
- 单文件 HTML（把整站塞进一个 `.html`）→ D-008
- 兄弟分支切换器（`← 2/3 →`）→ D-009
- 标签 / 云同步 / 插件化 / mermaid / 纯美化 → D-010
- 跨会话 token / 花费累计 → D-014
- 「把树总结成结论 / 要点」→ D-015（「价值提炼」指的是**产品交付的价值**，不是这个功能）
- 从上游 `Anionex/treeAI` 合并 → 已分家，不做；致谢见 `README.md` 的 Credits
